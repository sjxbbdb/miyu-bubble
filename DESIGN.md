# 设计说明

## 目标与非目标

**目标**：让一个只会在被叫时响应的终端 Agent，在用户长时间沉默后自己开口 ——
且说出来的话要**贴上下文、合时宜、有分寸**。

**非目标**：

- 不做通用调度框架。这是个单用途的小工具，不是 Airflow。
- 不做「任务催办」。目标是陪伴式的一句话，不是"你的活儿还没干完"。
- 不碰 miyu 的源码。全部走它已有的对外接口（`ask` / SQLite / systemd）。

## 三条主线决策

### 决策一：自调度循环，而不是 systemd timer

**候选方案**

| 方案 | 问题 |
|---|---|
| `systemd.timer` + `OnUnitActiveSec` | 固定周期，规律感强 |
| `systemd.timer` + `RandomizedDelaySec` | 抖动是**在周期上叠加的**，长期均值仍是固定周期 |
| **进程内 `sleep(BASE × random)`** | ✅ 每轮独立掷骰，无固定周期残留 |

**选第三条。** `RandomizedDelaySec` 看着够用，但它的随机是围绕固定周期抖动的 ——
统计上十分钟一次就是十分钟一次。而"想起来才说话"在行为上必须**没有可预测的节拍**。

代价：失去 systemd timer 的日历语义（不能写"每天 9 点"）。本工具不需要日历语义。

### 决策二：续接真实会话，而不是新开会话

**候选方案**

| 方案 | 问题 |
|---|---|
| 读会话历史 → 摘要 → `miyu ask "..."` 新会话 | 摘要是二手的；消息落在另一个会话里，上下文断开 |
| 让模型自己 `search_memory` 再生成 | 多烧 2–3 轮工具调用；一次主动问候要等 30–40 秒 |
| **`miyu ask -c` + 预注入上下文** | ✅ 上下文原生、落进正确会话、一次调用 |

**选第三条。** 关键发现是 `miyu ask` 有 `-c/--continue`：把消息发进**终端集成会话**
而不是一次性对话。这条接口让整个方案从"另起炉灶"变成"续上一轮"。

同时用 `--output-format json` 拿结构化终态。第一版是自己 sed 剥 ANSI 的，
结果把 `思考 · 30 词元 · 2.5s` 这行进度提示混进了消息正文 —— 换成 json 之后干净了，
还顺带拿到 `usage` 和 `context_tokens`/`context_window`（可以监控上下文水位）。

### 决策三：记忆直读 SQLite，不让模型自己检索

`~/.miyu/personas/default/memory/memory.db` 里：

- `episodes` —— 日记，格式 `时间戳，对方说：X；我回：Y`
- `facts` —— 事实，带 `strength` 权重

**直接 `select ... order by rowid desc limit 8`。**

理由：主动消息的**延迟预算很小**。让你等 40 秒才弹出来的关心，感觉像系统告警；
3 秒弹出来的，感觉像有人想起你。省下的工具调用轮次直接把延迟从 37 秒压到 3–6 秒。

代价：失去语义检索能力（`memory_embeddings` 那 64 条向量用不上）。
如果将来上下文预算收紧，可以考虑加一层按当前话题的向量召回。

## 上下文组装

喂进去的东西按顺序：

```
现在是 2026-09-23 22:16（周三，晚上）。

【最近发生的事（你自己的日记，由近到远）】
  · 2026-09-23T14:11:10+00:00，对方说：用一句话确认你还在；我回：在的，随时听大佬使唤
  · ...（共 8 条）

【你记得的事】
  · 截至 2026-09-23 用户一直在折腾 niri，是我在这个终端里的管理员
  · ...（共 8 条，按 strength 排序）
```

**时间单独成型**，不只是 ISO 字符串。`22:16` 对模型没有语义，`（周三，晚上）` 才有 ——
决定她会不会说出"这都十点多了"这种话的，正是这半行字。

时段划分：`05–11 早上 / 11–14 中午 / 14–18 下午 / 18–23 晚上 / 其余 深夜`。

## 提示词的分寸设计

```
3. 适度询问 —— 但不要追问、不要催活、不要提任何要求
6. 直接输出你要说的话本身，不要写「我来找你了」这类旁白
```

这两条是踩过的坑：

- **第 3 条**：不加约束时，模型会把"主动关心"漂移成"进度催办"——
  它会说"那个 KDE 清理搞定了吗"。功能性上没错，但性质变了：从陪伴变成监工。
- **第 6 条**：模型天然喜欢解释自己在干什么。不拦的话会输出
  "我注意到你已经 73 分钟没有输入了，所以我来找你"。**出戏，而且把机制暴露了。**

## 失败处理

| 失败点 | 处理 |
|---|---|
| 会话库读不到（文件锁 / 路径变） | `idle_seconds()` 返回 0 → 视为"用户刚说话" → 重掷不打扰 |
| 记忆库读不到 | `build_context()` 返回空 → **降级为仅时间**，并写日志 |
| `miyu` 退出码非 0 | 记日志，本轮放弃，继续下一轮 |
| `miyu` 返回空 text | 同上 |
| 通知投递失败 | 记日志（消息已在会话历史里，不算全丢） |
| 进程被 kill | `Restart=always` + `RestartSec=30` |

**注意第一版在"记忆库读不到"时是静默降级的** —— `build_context` 失败会让 `ctx` 为空，
而提示词照发。那等于用一个空提示词让模型自由发挥。现在会记日志并显式降级成时间上下文。

## 环境变量的坑

systemd 用户服务**不继承登录 shell 的环境**。两个必须显式给：

```ini
Environment=LANG=zh_CN.UTF-8                        # 不设这个 miyu 会用英文回你
Environment=DBUS_SESSION_BUS_ADDRESS=unix:path=/run/user/1000/bus   # notify-send 要靠它
```

第一条是实测踩出来的：`miyu` 的 `display.language = auto` 跟着 `LANG` 走，
而 systemd 服务的 `LANG` 默认是空的。

## 为什么不做"该不该说"的判断（现在）

用户明确要求「前期先做这个，后期再加判断」。

判断层的位置已经预留好了：在 `build_context` 之后、`miyu ask` 之前插一次轻量调用——
把上下文喂给一个短提示词，让它回答"现在适合打扰吗"。

**为什么现在不加**：判断层本身要先有足够样本才知道阈值定在哪。
在还没有观测数据之前加判断，只是把一个拍脑袋换成另一个拍脑袋。

## 实测发现：撞车不是失败，是排队

2026-09-23 用 `verify` 做链路验证时撞上了一次：我们的冒泡和一条正在飞的回合同时发生。
当时从会话库里读到的 `assistant_content` 是：

```
<system-reminder>上一轮prompt正在由另一轮回复处理中，你只需要回应用户当前的prompt</system-reminder>
```

**第一反应是"撞车失败了"。但十分钟后再看同一条记录，内容变成了：**

```
大佬还在扒我源码呢，查得这么细，是不是已经动 fork 的心思了
```

**所以 miyu 内部是排队的** —— 占位提醒先落库，等对方回合跑完后用真实回复**覆写**。
我们的 prompt 没有被丢弃，只是延迟送达。

**这对实现的三个影响：**

1. **占位符不能弹通知**。CLI 在那一刻返回的就是那段内部告警，
   用户看到会直接出戏。现在的处理是识别到占位符就跳过通知
   （消息其实已经进会话，只是不弹）。
2. **空闲判据要更严**。原来的实现只看 `user_timestamp`（用户最后开口），
   于是"用户问完就走开、小鱼还在答"这种情况下会插进去。
   现在取 `max(user_timestamp, assistant_timestamp)`，
   并且**任何一轮 `assistant_timestamp` 为空就直接判为不空闲**。
3. **不能靠 CLI 的返回判断成功**。CLI 返回占位符 ≠ 失败。
   真正的成功判据是会话库里那条记录最终变成了正常内容。

## 实测发现：SQLite 的 max(a, b) 不是聚合函数

改空闲判据时踩的坑。

想取「用户最后一次开口」和「小鱼最后一次说完」里较晚的那个，第一版写成：

```sql
select max(coalesce(user_timestamp,''), coalesce(assistant_timestamp,'')) from turns
```

**这是错的。** SQLite 里 `max()` 传**两个及以上参数**时是**标量函数** ——
它对每一行算出「这两个值里较大者」，而不是在整个结果集上求最大。
没有 GROUP BY 的情况下，`select` 列表里出现标量 `max()`，返回的是**任意一行**的值。

实测拿到 `2026-09-22T17:09:59`，于是空闲被算成 **77624 秒（21.6 小时）** ——
**空闲判定永远通过，每轮掷完骰子立刻冒泡**，不管用户是不是正在聊。

正确写法要嵌套一层：

```sql
select max(m) from (
  select max(coalesce(user_timestamp,''), coalesce(assistant_timestamp,'')) as m
  from turns
)
```

内层逐行取较大者，外层聚合取最大。修完实测 63 秒，符合预期。

**教训**：这个 bug 不会报错、不会崩溃，只会让行为**静默地变得过于热情**。
唯一抓到它的方式是看日志里那行 `手动触发（当前空闲 77566s）` ——
如果当时只测"能不能发出消息"，它会完美通过。

## 实测发现：会话寻址是人格隔离的，而且指针会移动

用户反馈「冒泡跑到别的会话去了」，查出来的完整链条：

### ① miyu 有两条互不干扰的会话车道

源码里的注释写得很直白：

```rust
/// normal 车道永不落进终端集成会话(08-25 用户裁定)：指针缺失自举新会话
fn ensure_repl_session_never_lands_on_the_terminal_session()
```

也就是说：**每次开新的 REPL 都会新建一条会话**，而 `app_state` 表里的指针随之移动：

```
current_session                        = sess_1790175219851_cb71752f
current_session_persona:default        = default
current_session_persona:persona-8fa... = sess_1790175219851_cb71752f
```

`-c` 读的就是 `current_session_persona:<当前人格>`。

### ② 会话名是按人格隔离的

```rust
fn find_session_by_name_filtered(&self, persona: &str, name: &str, ...) {
    "SELECT ... FROM sessions
      WHERE persona = ?1 AND kind = 'user' AND name = ?2 COLLATE NOCASE
```

所以 `miyu ask --session "终端集成会话"` 在 `default` 人格下**失败** ——
那名字属于另一个 persona。而 `--session default`（用 session_id）也不行，
它只认名字和编号。编号还不稳定（列表按最近活动排序，每次都变）。

### ③ 结论：没有稳定的外部寻址方式

| 寻址方式 | 结果 |
|---|---|
| `-c` | 跟着「当前人格 × 当前会话」走 —— 会漂 |
| `--session <session_id>` | ❌ 不认（`default` 报"找不到该会话"） |
| `--session <名字>` | ⚠️ 只在**同人格**内有效 |
| `--session <编号>` | ⚠️ 不稳定，列表顺序会变 |

### ④ 因此加的配置

```bash
MIYU_BUBBLE_SESSION=""        # 留空 = 跟随当前会话（默认，见下方取舍）
```

| 取值 | 送到哪 | 取舍 |
|---|---|---|
| 留空 | 你当前所在的那条会话 | 一定送到你眼前，但**上下文可能很薄**（新会话没有历史） |
| 指定名字/编号 | 钉死在那条会话 | 上下文完整，但**你得自己保证会回去看** |

**默认留空**，因为"消息送到眼前"比"上下文完整"更重要 ——
而且 miyu 自己的记忆系统（episodes / facts）是跨会话的，
即使会话历史是空的，她仍然记得你们聊过什么。

## 未验证的部分

如实标注：

- **只在一台机器上测过**（Arch Linux + niri + kitty + fish + DMS 通知）。
  其他 compositor / 通知守护进程未验证。
- **长空闲路径未实测**。验证过的触发都是秒级（把 `BASE` 压到 15–20 秒）。
  真实的 22–90 分钟间隔下，系统休眠、会话过期、DB 锁竞争都会是新的变量。
- **上下文水位未观测到上限**。测试时 `context_tokens` 是 59K / 160K。
  涨到 80% 触发 compact 之后，记忆注入的效果是否还稳定，未知。
